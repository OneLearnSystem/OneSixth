import {rpc,signin,logout} from './api.mjs';import {parseNames,esc,csv} from './core.mjs';
const app=document.querySelector('#app'),by=id=>document.getElementById(id),brand='<div class="brand"><img src="logo.svg" alt="OneSixth logo">One<span>Sixth</span></div>';
let mode='student',data=null,view='home',code='',selected=null,preview=null,filter='',yearFilter='',busy=false;
const admin=()=>data?.role==='admin',people=()=>data.students.filter(s=>!s.archived),full=s=>s.first+' '+s.last,sub=id=>data.subjects.find(s=>s.id===id)?.name||id,group=c=>`${c.year} ${sub(c.subject_id)} · ${String.fromCharCode(64+c.section)}`,ownClasses=id=>data.classes.filter(c=>data.enrolments.some(e=>e.student_id===id&&e.class_id===c.id)),today=()=>new Date().toLocaleDateString('en-CA',{timeZone:'Europe/London'});
function toast(s){by('toast').textContent=s;by('toast').className='show';setTimeout(()=>by('toast').className='',4500)}
async function action(el,fn){if(busy)return;busy=true;if(el)el.disabled=true;try{await fn()}catch(e){toast(e.message)}finally{busy=false;if(el?.isConnected)el.disabled=false}}
async function refresh(){data=await rpc(mode==='staff'?'ss_staff':'ss_student',mode==='staff'?{}:{p_code:code},mode==='staff');render()}
const head=(title,text,btn='')=>`<header class="pagehead"><div><span class="eyebrow">YOUR SCHOOL · SIXTH FORM</span><h1>${title}</h1><p>${text}</p></div>${btn}</header>`,empty=t=>`<div class="empty">${t}</div>`;
function login(error=''){app.innerHTML=`<div class="login"><section class="intro">${brand}<h1>Your choices.<br><em>Your next chapter.</em></h1><p>A new space to grow, explore and become. Welcome to your sixth form.</p><div class="mini-grid"><div><b>12</b>Find your feet</div><div><b>13</b>Go further</div><div><b>14</b>Your pathway</div></div></section><section class="signin"><form id="login"><div class="switch"><button type="button" data-mode="student" class="${mode==='student'?'selected':''}">Student</button><button type="button" data-mode="staff" class="${mode==='staff'?'selected':''}">Staff</button></div><span class="eyebrow">WELCOME TO ONESIXTH</span><h1>${mode==='staff'?'Make room for<br>their potential.':'Your future starts here.'}</h1><p class="muted">${mode==='staff'?'Sign in with your existing OneEducation staff account.':'Use your OneSixth student login code. Your access code is a separate campus identifier.'}</p>${mode==='staff'?'<label for="email">Staff email</label><input id="email" type="email" autocomplete="username" required><label for="password">Password</label><input id="password" type="password" autocomplete="current-password" required>':'<label for="code">Student login code</label><input id="code" autocomplete="off" spellcheck="false" required placeholder="Paste your login code">'}<button class="full" type="submit">Enter OneSixth →</button><p id="loginError" class="error">${esc(error)}</p><p class="footnote">OneSixth · Part of One Education<br>Need your code? Ask a member of sixth-form staff.</p></form></section></div>`;document.querySelectorAll('[data-mode]').forEach(b=>b.onclick=()=>{mode=b.dataset.mode;login()});by('login').onsubmit=async e=>{e.preventDefault();e.submitter.disabled=true;try{if(mode==='staff')data=await signin(by('email').value.trim(),by('password').value);else{code=by('code').value.trim();data=await rpc('ss_student',{p_code:code});sessionStorage.setItem('onesixth-code',code)}view='home';render()}catch(err){by('loginError').textContent=err.message;e.submitter.disabled=false}}}
function render(){const nav=mode==='student'?[['home','My home'],['choices','My subject choices'],['classes','My classes'],['campus','A Block & access'],['notices','Announcements'],['attendance','My attendance']]:[['home','Overview'],['students','Students'],...(admin()?[['import','Mass import']]:[]),['classes','Classes & subjects'],['register','Attendance'],['notices','Announcements'],['campus','A Block'],...(admin()?[['settings','Administration']]:[])];app.innerHTML=`<div class="shell"><aside>${brand}<nav>${nav.map(([id,n])=>`<button data-nav="${id}" class="${view===id?'active':''}">${n}</button>`).join('')}</nav><div class="rail-note"><strong>A place for your next chapter.</strong><br>your sixth form · A Block<br>Connected staff accounts. Dedicated sixth-form records.</div></aside><section class="workspace"><header class="topbar"><div><strong>${esc(data.settings.school_name)}</strong><small>${mode==='student'?`Year ${data.student.year} · ${esc(full(data.student))}`:admin()?'Administrator workspace':'Staff workspace'}</small></div><div class="actions"><span class="avatar">${mode==='student'?esc(data.student.first[0]):'S'}</span><button class="plain" id="refresh">Refresh</button><button class="plain" id="logout">Sign out</button></div></header><main class="content">${page()}</main></section></div>`;document.querySelectorAll('[data-nav]').forEach(b=>b.onclick=()=>{view=b.dataset.nav;preview=null;filter='';yearFilter='';render()});by('logout').onclick=()=>{logout();data=null;code='';login()};by('refresh').onclick=e=>action(e.currentTarget,refresh);bind()}
const stats=rows=>`<div class="stats">${rows.map(([t,v])=>`<div class="stat"><small>${t}</small><strong>${v}</strong></div>`).join('')}</div>`;
const notices=rows=>rows.length?`<div class="grid">${rows.map(n=>`<article class="card"><div class="card-head"><span class="tag">${n.year?'Year '+n.year:'Everyone'}${n.published?'':' · Draft'}</span><small>${new Date(n.created_at).toLocaleDateString('en-GB')}</small></div><h3>${esc(n.title)}</h3><p style="white-space:pre-line">${esc(n.body)}</p>${mode==='staff'?`<button class="secondary mini" data-notice="${n.id}">Edit notice</button>`:''}</article>`).join('')}</div>`:empty('No announcements yet.');
function courseChoices(ids=[]){return ['option','core'].map(g=>`<h3>${g==='option'?'Choose three options':'Choose two core subjects'}</h3><div class="subject-grid">${data.subjects.filter(s=>s.group_type===g).map(s=>`<label class="card selectable"><input type="checkbox" name="subject" value="${s.id}" ${ids.includes(s.id)?'checked':''}> ${esc(s.name)}<small>A level · ${g==='option'?'Option subject':'Core choice'}</small></label>`).join('')}</div>`).join('<br>')}
function validateChoices(allowEmpty=false){const ids=[...document.querySelectorAll('[name=subject]:checked')].map(c=>c.value);if(allowEmpty&&!ids.length)return null;if(ids.length!==5||ids.filter(id=>data.subjects.find(s=>s.id===id)?.group_type==='core').length!==2)throw Error('Choose exactly three options and two core subjects.');return ids}
function classesList(cs){return cs.length?`<div class="grid">${cs.map(c=>`<article class="card"><span class="tag">Year ${c.year} · A level</span><h3>${esc(sub(c.subject_id))}</h3><p>${esc(group(c))}<br>Home room ${c.room} · A Block</p>${mode==='staff'?`<div class="card-foot"><small>${data.enrolments.filter(e=>e.class_id===c.id).length} / ${c.capacity} places</small><button class="secondary" data-class="${c.id}">View students</button></div>`:''}</article>`).join('')}</div>`:empty('No classes yet. Choose your subjects to be allocated to matching groups.')}
function page(){if(view==='campus')return head('A new space. A fresh start.','Your sixth-form home in the newly built A Block.')+`<div class="grid two"><article class="card"><span class="eyebrow">A BLOCK · SIX CLASSROOMS</span><h2>Find your way around.</h2><div class="campus-plan"><div>A01</div><div>A02</div><div>A03</div><div class="corridor">CENTRAL CORRIDOR · MAIN ENTRANCE</div><div>A04</div><div>A05</div><div>A06</div></div><p class="footnote">Illustrative layout. Classes have a home room; this is not a lesson timetable or evacuation plan.</p></article><article class="card"><h2>${mode==='student'?'Your sixth-form access card':'Campus access codes'}</h2>${mode==='student'?`<p>${esc(full(data.student))} · Year ${data.student.year}</p><div class="portal-code">${esc(data.student.access_code)}</div><p>Show this identifier to sixth-form staff when asked.</p>`:'<p>Each imported student receives a unique access identifier, separate from their private portal login code. Find it in their student record.</p>'}<div class="notice">Access codes identify students within this system. They do not unlock physical doors or connect to access-control hardware.</div></article></div>`;
if(view==='notices')return head('In the loop.','Updates for our sixth-form community.',mode==='staff'?'<button id="newNotice">+ New announcement</button>':'')+notices(data.notices);
if(view==='noticeEdit'){const n=data.notices.find(n=>n.id===selected)||{};return head('Share an update.','Publish to everyone or a specific year group.')+`<form id="noticeForm" class="card"><label>Title<input id="noticeTitle" maxlength="160" value="${esc(n.title)}" required></label><label>Audience<select id="noticeYear"><option value="">Everyone</option>${[12,13,14].map(y=>`<option ${n.year===y?'selected':''}>${y}</option>`).join('')}</select></label><label>Message<textarea id="noticeBody" maxlength="10000" required>${esc(n.body)}</textarea></label><label class="inline-check"><input id="noticePublished" type="checkbox" ${n.published!==false?'checked':''}> Published</label><button>Save announcement</button></form>`}
if(view==='choices')return head('Five choices. Your direction.','Choose three options and two core subjects. We’ll place you into matching classes.')+`<form id="choicesForm" class="card">${data.settings.choices_open?'':'<div class="notice">Choices are closed. Staff can help with changes.</div>'}${courseChoices(data.classes.map(c=>c.subject_id))}<div class="notice">Your school’s five-subject model. Discuss your combination and GCSE suitability with staff. Year placement is selected by staff, not automatically decided by grades.</div><button ${!data.settings.choices_open?'disabled':''}>Save choices & allocate classes</button></form>`;
if(view==='classes'){const cs=mode==='student'?data.classes:data.classes.filter(c=>(!yearFilter||c.year===Number(yearFilter))&&group(c).toLowerCase().includes(filter.toLowerCase()));return head(mode==='student'?'Your learning community.':'Classes, ready for your students.','Matching subject groups across Years 12, 13 and 14. Home rooms are shared; lesson scheduling is separate.')+(mode==='staff'?filters():'')+classesList(cs)}
if(view==='classRoster'){const c=data.classes.find(c=>c.id===selected),ids=data.enrolments.filter(e=>e.class_id===selected).map(e=>e.student_id);return head(esc(group(c)),`A Block · ${c.room} · Capacity ${c.capacity}`)+studentTable(people().filter(s=>ids.includes(s.id)))}
if(view==='students')return head('People, not just records.','Find students, access identifiers and subject choices.',admin()?'<button id="exportCodes">Export student codes</button>':'')+filters()+studentTable(people().filter(s=>(!yearFilter||s.year===Number(yearFilter))&&[full(s),s.access_code].join(' ').toLowerCase().includes(filter.toLowerCase())));
if(view==='student'){const s=data.students.find(s=>s.id===selected),cs=ownClasses(s.id);return head(esc(full(s)),`Year ${s.year} · ${s.archived?'Archived':'Current student'}`)+`<div class="grid two"><article class="card"><span class="eyebrow">CAMPUS ACCESS IDENTIFIER</span><div class="portal-code">${esc(s.access_code)}</div>${admin()?`<label>Private portal login code</label><p class="codes">${esc(s.login_code)}</p><button class="secondary mini" id="rotate">Regenerate both codes</button>`:''}</article><article class="card"><h3>GCSE / placement notes</h3><p style="white-space:pre-line">${esc(s.gcse_notes||'No notes recorded.')}</p></article></div>${admin()?`<form id="editStudent" class="card" style="margin-top:20px"><div class="form-grid"><label>Year group<select id="editYear">${[12,13,14].map(y=>`<option ${s.year===y?'selected':''}>${y}</option>`).join('')}</select></label><label>GCSE / placement notes<textarea id="editGcse" maxlength="2000">${esc(s.gcse_notes)}</textarea></label></div>${courseChoices(cs.map(c=>c.subject_id))}<label class="inline-check"><input id="archived" type="checkbox" ${s.archived?'checked':''}> Archive student (revokes portal access and removes class places)</label><button>Save student</button></form>`:classesList(cs)}`}
if(view==='import')return head('Their next chapter starts here.','Import names, select the year, and generate private login and access codes.')+`<form id="importForm" class="card"><div class="form-grid"><label>Year group<select id="importYear"><option>12</option><option>13</option><option>14</option></select></label><label>Name order<select id="order"><option value="first-last">First name – Last name</option><option value="last-first">Last name – First name</option></select></label></div><label>Load a CSV / text file<input id="nameFile" type="file" accept=".csv,.txt,.tsv"></label><label>Student names<textarea id="names" placeholder="Amelia, Bennett&#10;Noah, Patel" required></textarea></label><p class="hint">One student per line. Use two CSV columns for compound names. First name / Last name headers are recognised. Up to 500 at a time.</p><label>GCSE / placement notes for this batch (optional)<textarea id="batchGcse" maxlength="2000"></textarea></label><details><summary>Set the same five subjects for this batch (optional)</summary><p class="hint">Leave all unticked to let students choose in their portals.</p>${courseChoices()}</details><div class="form-actions"><button type="button" id="previewImport">Preview names</button><button id="importButton" type="submit">Import students & generate codes</button></div><div id="importPreview"></div><div class="notice">Names already active in the same year are skipped. Students with identical names need a distinguishing middle name. Existing MIS student records are not altered.</div></form>`;
if(view==='settings')return head('Your sixth form, your settings.','Administrator controls.')+`<article class="card"><h2>Student subject selection</h2><p>${data.settings.choices_open?'Open: students can choose or change subjects.':'Closed: only administrators can change subjects.'}</p><button id="toggleChoices">${data.settings.choices_open?'Close':'Open'} student choices</button><p class="hint">Changes are checked in the database, including when a student already has the portal open.</p></article><section class="section" style="margin-top:24px"><h2>Archived students</h2>${studentTable(data.students.filter(s=>s.archived))}</section>`;
if(view==='register')return registerPage();if(view==='attendance')return head('Your attendance.','Recorded morning and afternoon sessions.')+`<article class="card table-wrap"><table><thead><tr><th>Date</th><th>Session</th><th>Mark</th><th>Notes</th></tr></thead><tbody>${data.attendance.map(a=>`<tr><td>${esc(a.date)}</td><td>${a.session}</td><td>${esc(a.mark)}</td><td>${esc(a.note)}</td></tr>`).join('')}</tbody></table>${data.attendance.length?'':empty('No attendance recorded yet.')}</article>`;
return head(mode==='student'?`Hello, ${esc(data.student.first)}.`:'The next chapter, together.',mode==='student'?'Your choices, classes and sixth-form community.':'One workspace for Your school’s sixth-form students.')+`<section class="hero"><div><span class="eyebrow">WELCOME TO ONESIXTH</span><h2>Room to grow.<br>Space to become.</h2><p>${mode==='student'?'Explore your subjects, find your A Block classrooms, and keep up with the latest announcements.':'Import your students, guide their choices, and help every learner find their next step.'}</p></div><div class="hero-mark">6</div></section>`+stats(mode==='student'?[['Year group',data.student.year],['My subjects',data.classes.length],['Home block','A']]:[['Current students',people().length],['Awaiting choices',people().filter(s=>!data.enrolments.some(e=>e.student_id===s.id)).length],['Classrooms',6]])+(mode==='student'&&data.classes.length===0?'<div class="notice">Choose your five subjects to join your classes. Open “My subject choices”.</div>':'')+`<h2>Latest announcements</h2>${notices(data.notices.slice(0,4))}`}
function filters(){return `<div class="filters"><input id="filter" placeholder="Search…" aria-label="Search" value="${esc(filter)}"><select id="yearFilter" aria-label="Year group"><option value="">All years</option>${[12,13,14].map(y=>`<option ${yearFilter===String(y)?'selected':''}>${y}</option>`).join('')}</select></div>`}
function studentTable(rows){return rows.length?`<article class="card table-wrap"><table><thead><tr><th>Student</th><th>Year</th><th>Access code</th><th>Subjects</th><th></th></tr></thead><tbody>${rows.map(s=>`<tr><td>${esc(full(s))}</td><td>${s.year}</td><td class="codes">${esc(s.access_code)}</td><td>${ownClasses(s.id).length} / 5</td><td><button class="secondary mini" data-student="${s.id}">Open record</button></td></tr>`).join('')}</tbody></table></article>`:empty('No students to show.')}
function registerPage(){return head('Every session counts.','AM and PM registration. Closed-school rules follow your main MIS.')+`<form id="registerForm" class="card"><div class="form-grid"><label>Date<input id="regDate" type="date" max="${today()}" value="${today()}" required></label><label>Session<select id="regSession"><option>AM</option><option>PM</option></select></label></div><label>Year group<select id="regYear"><option>12</option><option>13</option><option>14</option></select></label><div id="registerRows" class="table-wrap"></div><button>Save register</button><p class="hint">Only rows with a chosen mark are saved. Unmarked students stay unrecorded.</p></form>`}
function bind(){const on=(id,event,fn)=>by(id)?.addEventListener(event,fn);on('filter','input',e=>{filter=e.target.value;const pos=e.target.selectionStart;render();by('filter').focus();by('filter').setSelectionRange(pos,pos)});on('yearFilter','change',e=>{yearFilter=e.target.value;render()});document.querySelectorAll('[data-student]').forEach(b=>b.onclick=()=>{selected=b.dataset.student;view='student';render()});document.querySelectorAll('[data-class]').forEach(b=>b.onclick=()=>{selected=b.dataset.class;view='classRoster';render()});document.querySelectorAll('[data-notice]').forEach(b=>b.onclick=()=>{selected=b.dataset.notice;view='noticeEdit';render()});on('newNotice','click',()=>{selected=null;view='noticeEdit';render()});
on('choicesForm','submit',e=>{e.preventDefault();action(e.submitter,async()=>{data=await rpc('ss_choose',{p_code:code,p_subjects:validateChoices()});view='classes';render();toast('Your choices and classes are saved.')})});
on('nameFile','change',e=>action(e.target,async()=>{const f=e.target.files[0];if(f){if(f.size>1000000)throw Error('Use a file smaller than 1 MB.');by('names').value=await f.text()}}));
on('previewImport','click',e=>action(e.target,async()=>{preview=parseNames(by('names').value,by('order').value);by('importPreview').innerHTML=`<div class="import-preview"><p>${preview.length} valid names · Year ${esc(by('importYear').value)}</p>${preview.slice(0,50).map(n=>`<p>${esc(full(n))}</p>`).join('')}</div>`}));
on('importForm','submit',e=>{e.preventDefault();action(e.submitter,async()=>{const result=await rpc('ss_import',{p_names:parseNames(by('names').value,by('order').value),p_year:Number(by('importYear').value),p_subjects:validateChoices(true),p_gcse:by('batchGcse').value},true);view='students';await refresh();toast(`${result.imported} imported; ${result.skipped} existing names skipped. Export codes to distribute logins.`)})});
on('editStudent','submit',e=>{e.preventDefault();action(e.submitter,async()=>{await rpc('ss_edit_student',{p_id:selected,p_year:Number(by('editYear').value),p_subjects:by('archived').checked?null:validateChoices(true),p_gcse:by('editGcse').value,p_archived:by('archived').checked},true);await refresh();toast('Student updated.')})});
on('rotate','click',e=>{if(confirm('Replace both codes? The old login and access codes will stop working.'))action(e.target,async()=>{await rpc('ss_rotate',{p_id:selected},true);await refresh();toast('New codes generated. Distribute them privately.')})});
on('noticeForm','submit',e=>{e.preventDefault();action(e.submitter,async()=>{await rpc('ss_notice',{p_title:by('noticeTitle').value,p_body:by('noticeBody').value,p_year:by('noticeYear').value?Number(by('noticeYear').value):null,p_id:selected,p_published:by('noticePublished').checked},true);view='notices';await refresh();toast('Announcement saved.')})});
on('toggleChoices','click',e=>action(e.target,async()=>{await rpc('ss_settings_save',{p_open:!data.settings.choices_open},true);await refresh()}));
on('exportCodes','click',()=>{const text=csv([['First name','Surname','Year','Private login code','Access code'],...people().map(s=>[s.first,s.last,s.year,s.login_code,s.access_code])]);const u=URL.createObjectURL(new Blob([text],{type:'text/csv;charset=utf-8'})),a=document.createElement('a');a.href=u;a.download='OneSixth-student-codes.csv';a.click();setTimeout(()=>URL.revokeObjectURL(u),1000)});
if(by('registerForm')){const rows=()=>{by('registerRows').innerHTML=`<table><thead><tr><th>Student</th><th>Mark</th><th>Notes</th></tr></thead><tbody>${people().filter(s=>s.year===Number(by('regYear').value)).map(s=>{const a=data.attendance.find(a=>a.student_id===s.id&&a.date===by('regDate').value&&a.session===by('regSession').value);return `<tr data-reg="${s.id}"><td>${esc(full(s))}</td><td><select class="mark" aria-label="Mark for ${esc(full(s))}"><option value="">Unrecorded</option>${['present','absent','late','ill','authorised'].map(v=>`<option ${a?.mark===v?'selected':''}>${v}</option>`).join('')}</select></td><td><input class="note" aria-label="Notes for ${esc(full(s))}" maxlength="1000" value="${esc(a?.note)}"></td></tr>`}).join('')}</tbody></table>`};['regYear','regDate','regSession'].forEach(id=>on(id,'change',rows));rows();on('registerForm','submit',e=>{e.preventDefault();action(e.submitter,async()=>{const marks=[...document.querySelectorAll('[data-reg]')].filter(r=>r.querySelector('.mark').value).map(r=>({id:r.dataset.reg,mark:r.querySelector('.mark').value,note:r.querySelector('.note').value}));await rpc('ss_register',{p_date:by('regDate').value,p_session:by('regSession').value,p_marks:marks},true);data=await rpc('ss_staff',{},true);toast('Register saved.')})})}}
login();
